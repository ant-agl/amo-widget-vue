{
  "name": "vue-widget",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build --mode production && npm run replace-widget-path",
    "build-dev": "vite build --mode development && npm run replace-widget-path-dev",
    "build-watch": "vite build --watch --mode development",
    "replace-widget-path": "node replaceWidgetPath.js --mode production",
    "replace-widget-path-dev": "node replaceWidgetPath.js --mode development",
    "server": "npx serve dist",
    "dev": "npm run build-watch & npm run server",
    "bump-version": "node bumpVersion.js",
    "cp-widget": "rm -rf widget && cp -r dist widget",
    "zip": "cd dist && zip -r ../widget.zip *",
    "zip-widget": "npm run cp-widget && npm run zip",
    "widget": "npm run bump-version && npm run build && npm run zip-widget",
    "widget-dev": "npm run bump-version && npm run build-dev && npm run zip-widget && npm run dev"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "pinia": "^2.2.6",
    "sass": "^1.80.6",
    "vue": "^3.5.12"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.4",
    "dotenv": "^16.4.5",
    "vite": "^5.4.10",
    "yargs": "^17.7.2"
  }
}